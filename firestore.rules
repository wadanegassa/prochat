rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow authenticated users read/write access to all collections
    // This is for debugging the "Auth not working" issue
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
